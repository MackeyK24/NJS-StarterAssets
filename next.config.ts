import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // output: 'export', // Note: To force Next.js to emit a fully static site to the `out` directory (preview: npx serve out)
};

export default nextConfig;
